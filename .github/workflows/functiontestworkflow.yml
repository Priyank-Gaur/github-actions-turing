name: Function Test Workflow
on:
  push:
jobs:
  function-test:
    runs-on: ubuntu-latest
    steps:
      - name: Repo Clone
        uses: actions/checkout@v4
      - name: Run Function Tests
        run: node index.js
